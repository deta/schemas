&root
- name: Test tests schema
  description: Test the schema that validates this file
  valid: true
  schema: tests
  version: 0
  data: *root

- name: Spacefile test 1
  description: valid spacefile
  valid: true
  schema: spacefile
  version: 0
  data:
    v: 0
    micros:
      - name: micro-name
        src: .
        engine: custom
        primary: true
        path: micro/url
        commands:
          - command
        include:
          - file
        run: executable

- name: Invalid version
  description: Tests invalid value for 'v' attribute
  valid: false
  schema: spacefile
  version: 0
  data:
    v: 1
    micros:
      - name: micro-name
        src: .
        engine: custom
        primary: true
        path: micro/url
        commands:
          - command
        include:
          - file
        run: executable
